{
  "name": "pulumi-graph",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "pulumi-graph": "tsx ./src/index.ts --",
    "ui:build": "npm --prefix web install --no-audit --no-fund && npm --prefix web run build",
    "typecheck": "tsc --noEmit",
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  },
  "dependencies": {
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "prettier": "^3.3.3",
    "tsx": "^4.21.0",
    "typescript": "^5.5.3"
  },
  "private": true
}
